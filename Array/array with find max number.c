#include<stdio.h>

int main(int argc, char const *argv[])
{
    int x;
    scanf("%d",&x);

    int to[];
    int count=0;

    for (int i=0;i<x;i++)
    {
        to[]= to[i]);
    }

      for (int j=0;i<x;i++)
    {
        count=count+to[i];
    }

    printf("%d",count);



    return 0;
}
